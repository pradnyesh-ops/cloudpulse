# CloudPulse — Python Dependencies

# AWS SDK
boto3==1.34.144
botocore==1.34.144

# Web framework & real-time
flask==3.0.3
flask-cors==4.0.1
gevent==24.2.1          # SSE / async workers

# Data processing
pyspark==3.5.1           # local Spark; on EMR the cluster version is used
pandas==2.2.2
numpy==1.26.4

# HTTP / streaming data sources
requests==2.32.3
sseclient-py==1.8.0      # consume RIPE Atlas / Wikimedia SSE feeds

# Serialisation & schema
python-dateutil==2.9.0
pytz==2024.1

# Configuration
python-dotenv==1.0.1

# Visualisation helpers (server-side chart data)
matplotlib==3.9.0        # used in benchmarks only

# Testing & benchmarking
locust==2.28.0
pytest==8.2.2
